{
  "schema_version": 1,
  "entries": [
    {
      "id": 1,
      "date": "2026-09-01",
      "severity": "S2",
      "finding": "The intake path normalizes every open issue into a repository-mutating atom contract or blocks it, so a non-actionable caveat or a disclosed-not-fixed finding cannot exist as an issue; across wave 9 every such item degraded into prose on a record that then closed, and three lanes independently rediscovered the same hole.",
      "receipts": [
        "noodles.py:intake_normalize - every open issue read by `.noodle/adapters/github sync` is PATCHed into the atom contract or blocked with an intake comment",
        "ed3c/noodles#263 body - the wave-9 clause-compliance judge's rank-1 gap, S2, that filed this register"
      ],
      "owner_component": "schedule",
      "status": "promoted",
      "promoted_to": "ed3c/noodles#263",
      "chain": "842e8d1ee3658f1f153e22d458f7bed016485615db5d5b4627ec20c20078e7a9"
    },
    {
      "id": 2,
      "date": "2026-09-01",
      "severity": "S2",
      "finding": "The one physical instance behind the staged-transition rule - PR ed3c/noodles#129 could not go green because trusted verify runs main's copy of a test over the candidate tree - is recorded only inside the bodies of ed3c/noodles#252 and ed3c/noodles#253; when those close, the rule in contracts/system-v1.md keeps its statement and loses its evidence.",
      "receipts": [
        "ed3c/noodles#252 body - PR ed3c/noodles#129 verify run 33366751879, job `Run trusted positive and planted-negative controls`, failure at .trusted/tests/test_noodles.py:298",
        ".github/workflows/verify.yml - PYTHONPATH .trusted with NOODLES_CANDIDATE_ROOT .candidate is why main's copy judges the candidate",
        "contracts/system-v1.md staged-transition paragraph - states the rule with no receipt attached"
      ],
      "owner_component": "docs",
      "status": "promoted",
      "promoted_to": "ed3c/noodles#274",
      "chain": "189f7d9b89471f1c3cd22bde87a516eb6ee3104dbfa2080358ff0ec92342d2f8"
    },
    {
      "id": 3,
      "date": "2026-09-01",
      "severity": "S2",
      "finding": "`./noodles issue handoff` carries the journey-compilation gate but the merge-gating verify path never calls it, and real lanes reach awaiting_land by flipping the state marker directly, so the gate landed by ed3c/noodles#242 is enforced only on a route those lanes physically cannot take.",
      "receipts": [
        "noodles.py:execute_handoff is the only caller of feature_contract.compile_handoff_feature_map",
        "noodles.py:verify_pull_request carries zero feature_contract references",
        "merged PRs fix-46-worktree-admission, fix-99-exclusive-admission, fix-100-concurrency-invariants - none uses the ed3c-noodles-N-0-execute branch that noodles.execute_provenance_admission requires"
      ],
      "owner_component": "verify",
      "status": "promoted",
      "promoted_to": "ed3c/noodles#264",
      "chain": "96aef24c0c341c41acffe52a32a5a958bb37be5b396badfc9cbd7b559cfdcd4c"
    },
    {
      "id": 4,
      "date": "2026-09-01",
      "severity": "S2",
      "finding": "Five open issues declare a `noodles-feature` id that feature_contract.resolve_feature refuses, and nothing on the landing route resolves the marker, so an unadmitted specialized-oracle id stays invisible until someone runs a route no lane takes.",
      "receipts": [
        "feature_contract.ADMITTED_FEATURES holds exactly verification-skill-oracle, metrics-cli-oracle, repo-infra-verify-oracle, landing-train-verify-aware-selection, land-time-receipt-anchor",
        "ed3c/noodles#257 component-surface-import-edges, #260 codex-isolation-truncation, #263 findings-register, #264 handoff-route-truth, #268 component-surface-file-overlap - none of those five ids is admitted",
        "noodles.py:verify_pull_request never calls feature_contract.resolve_feature; AGENTS.md states unknown feature ids fail at completion, which is a route wave lanes do not take"
      ],
      "owner_component": "verify",
      "status": "open",
      "chain": "63300725ff6f302ab7031c16021a842751f32331824930a16eb8fdf703941566"
    },
    {
      "id": 5,
      "date": "2026-09-01",
      "severity": "S2",
      "finding": "ed3c/noodles#264 makes an unadmitted `noodles-feature` id a land-time refusal, so the five open issues carrying one (#257, #260, #263, #264, #268) are refused by the trusted verify gate once that atom is on the default branch; the diagnostic names the cure but nothing applies it for them.",
      "receipts": [
        "noodles.py:verify_pull_request - `candidate feature-journey gate failed: ... Supported path: ... or drop the noodles-feature marker from <subject>`",
        "tests/test_feature_journey_route.py::test_planted_negative_unadmitted_feature_id_is_refused_naming_the_supported_path",
        "register entry 4 - the same five ids, recorded before this atom made them load-bearing"
      ],
      "owner_component": "verify",
      "status": "open",
      "chain": "f22c7dc41c09f4701feef54acfae377b2b72d69b21c3c1e44480401e3a73f15d"
    },
    {
      "id": 6,
      "date": "2026-09-01",
      "severity": "S3",
      "finding": "ed3c/noodles#272 routed `repair_contract.find_open_pr_for_subject` through `noodles.matching_open_pull_requests`, which makes that function's own `constraint:` comment (\"repair_contract.find_open_pr_for_subject does not route through this exit yet\") false; the atom declares component `verify` and `policy/component-owners.json` gives `matching_open_pull_requests` to `schedule`, so `component_owner_errors` refuses the candidate that would correct the comment. Any future `schedule`-declared atom touching that definition should delete those four lines.",
      "receipts": [
        "noodles.py:matching_open_pull_requests - the four stale `# constraint:` lines naming ed3c/noodles#272",
        "policy/component-owners.json - `\"matching_open_pull_requests\": [\"schedule\"]`",
        "noodles.py:component_owner_errors - refuses a changed definition owned by an undeclared component",
        "tests/test_repair.py::RepairOpenPrCorrelationTests - the routing the comment now contradicts"
      ],
      "owner_component": "schedule",
      "status": "open",
      "chain": "fa969216fd0829295448203c39c20ce0c9af7a9a3db4e7e83bb59b409bb1c8bc"
    },
    {
      "id": 7,
      "date": "2026-09-01",
      "severity": "S3",
      "finding": "`tests/test_component_surface.py::ComponentOwnerMapTests.test_noodles_py_unowned_definition_count_matches_the_agents_md_disclosure` pins `assertEqual(len(owned), 18)` on `policy/component-owners.json`, so no candidate can add an owner entry: trusted verify and `./noodles verify --trusted-preview` run the default-branch copy of that test against the candidate's data, exactly the deadlock ed3c/noodles#285 widened out of the sibling assertion in the same method (`assertGreaterEqual(len(defs), 139)`). Reproduced while ed3c/noodles#272 tried to declare `claimed_boundary_widening`'s owner and had to drop the entry instead. Cure is the same one-word widening to a monotonic floor, which still catches a deleted owner entry.",
      "receipts": [
        "tests/test_component_surface.py:330 - `self.assertEqual(len(owned), 18)`",
        "tests/test_component_surface.py:318-325 - the ed3c/noodles#285 docstring naming this exact deadlock for the sibling assertion",
        "reproduce: add one entry to policy/component-owners.json, then PYTHONPATH=. python3 -m unittest tests.test_component_surface -> `AssertionError: 19 != 18`"
      ],
      "owner_component": "verify",
      "status": "open",
      "chain": "72737a406f9f6838e1d5c474b19e6e32482da7e4a8d930fa9c311c561933dddc"
    },
    {
      "id": 8,
      "date": "2026-09-01",
      "severity": "S3",
      "finding": "`tests/test_schedule_contract.py::ScheduleContractTests.run_carrier` defaults `root=CANDIDATE_ROOT` and runs `.agents/bin/codex` with `cwd=CANDIDATE_ROOT`, so the carrier's own `isolated_home.mkdir(parents=True, exist_ok=True)` writes `.noodle/codex-isolation/{home,codex-home}` into the live checkout - `.noodle/` runtime state written by a test outside its own temporary directory, the exact class every atom's acceptance clause forbids. `.gitignore:3` keeps it out of any commit, so no candidate has ever been rejected for it and nothing reds. Cure: pass a `copy_tracked` root, which `mutated_copy` in the same class already builds.",
      "receipts": [
        "tests/test_schedule_contract.py:77 - `def run_carrier(self, argv, root: Path = CANDIDATE_ROOT)`, invoked at line 92 with `cwd=root`",
        ".agents/bin/codex:137-138 - `isolated_home.mkdir(parents=True, exist_ok=True)`",
        "reproduce: rm -rf .noodle/codex-isolation && PYTHONPATH=. python3 -m unittest tests.test_schedule_contract && ls .noodle -> `codex-isolation` reappears"
      ],
      "owner_component": "schedule",
      "status": "open",
      "chain": "f0926c59c44c05c7438ec247d87c5eb57bfbed9c9e5cc851900aa7a1e8a29002"
    },
    {
      "id": 9,
      "date": "2026-09-02",
      "severity": "S3",
      "finding": "ed3c/noodles#325 retired noodles.validate_task_profile_single_source into the ownership-key registry, but its policy/fitness.json key `task_profile_literal_exempt_paths` has to outlive it for one staging window: pull_request_target runs the DEFAULT BRANCH's copy of that function, which subscripts the key out of the CANDIDATE's own policy document, so a candidate that deletes the key raises KeyError inside the trusted verifier and reds every test that calls verify_repository. `./noodles verify --trusted-preview` reproduced exactly that on this atom's first head. The key now has no reader in the tree, so it is dead policy surface that policy/fitness.json still declares; the retirement atom deletes it once a default branch carrying skill_contract.validate_ownership_registry is what judges candidates (step 3 of widen -> flip -> retire). Until then a reader could re-appear and quietly become the second implementation the subsumption removed.",
      "receipts": [
        "policy/fitness.json - `task_profile_literal_exempt_paths` retained with no tracked .py reader",
        "skill_contract.py - the `# constraint: ed3c/noodles#325 staging window` block beside OWNERSHIP_REGISTRY_PATH is what keeps validate_policy_key_consumption green on a key nobody reads",
        "tests/test_agent_friendly_architecture.py::OwnershipRegistryTests::test_the_subsumed_task_profile_rule_leaves_exactly_one_implementation - asserts noodles.py names neither the function nor the key",
        "reproduce the deadlock: remove the key from policy/fitness.json, commit, then ./noodles verify --trusted-preview -> `test_codex_task_model_map_is_exact_and_drift_is_rejected` and `test_direct_readback_verify_json_reports_the_per_component_count` red with KeyError"
      ],
      "owner_component": "verify",
      "status": "open",
      "chain": "7ee8b97572f4beff8f1b423be6ccf70011819f5aeae3955e13d2bd509792a74f"
    }
  ]
}
